# Pandacea Protocol Worker Dependencies
# Pinned versions for production stability

# Core ML and DP libraries
torch==2.1.0
numpy==1.24.3
syft==0.5.0

# JSON schema validation
jsonschema==4.19.2

# Logging and utilities
python-dateutil==2.8.2

# Development dependencies (optional)
pytest==7.4.3
pytest-cov==4.1.0

# Note: PySyft may have compatibility issues on Windows
# Use Docker fallback if needed: docker run -it pysyft/pysyft:latest
